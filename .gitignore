node_modules
dist
.DS_Store
*.local

# Supabase credentials — never commit these
.env
.env.local

# build info
tsconfig.tsbuildinfo

# local backups
*.bak

*storybook.log
storybook-static

# storybook
storybook-static
